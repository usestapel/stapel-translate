{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "user.merged",
  "description": "Consumed from stapel-auth. An anonymous guest account was absorbed into an existing account; the guest row ceases to exist. Translate re-parents nothing: no model here is keyed by a user id — the two that name a person (AuthorizedTranslator, TranslationHistory) are addressed by email, which the payload does not carry and a guest session never has. The subscription exists so that 'nothing to move' is declared rather than inferred from silence. Contract owned by the emitter.",
  "type": "object",
  "required": ["from_user_id", "into_user_id"],
  "properties": {
    "from_user_id": { "type": "string" },
    "into_user_id": { "type": "string" },
    "reason": { "type": "string" }
  },
  "additionalProperties": true
}
